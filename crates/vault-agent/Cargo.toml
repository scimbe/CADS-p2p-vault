[package]
name = "vault-agent"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "vault-agent"
path = "src/main.rs"

[dependencies]
vault-manifest = { path = "../vault-manifest" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
base64 = "0.22"
